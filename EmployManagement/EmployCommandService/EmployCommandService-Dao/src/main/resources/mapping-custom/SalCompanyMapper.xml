<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ciicsh.gto.afsupportcenter.employmanagement.employcommandservice.dao.SalCompanyMapper">

	<!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMapDTO" extends="BaseResultMap"
               type="com.ciicsh.gto.afsupportcenter.employmanagement.employcommandservice.bo.SalCompanyBO">

    </resultMap>

    <select id="querySalCompanyList" resultMap="BaseResultMapDTO">
        SELECT  b.company_id,b.title  from  sal_company b
        where 1=1
        <if test="companyId!=null">
            and b.company_id = #{companyId}
        </if>
        <if test="title!=null">
            and b.title = #{title}
        </if>
    </select>


</mapper>
