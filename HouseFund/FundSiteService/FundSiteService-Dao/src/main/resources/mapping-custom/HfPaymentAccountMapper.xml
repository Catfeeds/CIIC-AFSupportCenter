<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ciicsh.gto.afsupportcenter.housefund.siteservice.dao.HfPaymentAccountMapper">

    <select id="getByPaymentId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        payment_account_id,
        payment_id,
        com_account_id,
        payment_bank,
        payment_status,
        total_com_pay_amount,
        total_emp_pay_amount,
        is_active,
        created_time,
        modified_time,
        created_by,
        modified_by
        FROM
        hf_payment_account SPC
        WHERE
        SPC.is_active = '1'
        AND SPC.payment_id = #{paymentId}
    </select>

</mapper>
